/**
 * @author : Darshit Karkar
 * @description : nameValidator component
 * @param : props
 * @university : University of Regina
 */
export function nameValidator(name) {
  if (!name) return "Name can't be empty."
  return ''
}
